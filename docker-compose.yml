version: '3.3'
services:
    database:
        ports:
            - '5333:5332'
        environment:
            - POSTGRES_PASSWORD=1234
        image: 'postgres:12.2'
        depends_on:
          - webapp

    webapp:
      build: .
      ports:
        - '4000:5000'

